## Kế hoạch hoàn thiện nốt CP5 → CP7

### 1. Đổi Q4 thành câu bẫy A/B thật (R2 chốt, bạn đã chọn phương án)
- Thay Q4 trong `bench.py` + REPORT_NHOM mục 3 bằng: *"How do I borrow and pick up library items when I cannot visit the library myself due to a disability?"*
  - Gold: `utsc-borrowing-policy` (student) — mục "Designate a proxy borrower": "...available to any library borrower unable to visit... Complete this application form (UTORid login required)".
  - Bẫy: `utsc-accessibility-services` (all) có mục "Proxy borrower authorization" nói **Pickup Authorization Form ở Info Desk** — khác đáp án → không lọc thì hai tài liệu lẫn nhau.
  - Marker chấm nội dung: "application form".
- Chạy lại để **xác minh A/B ra khác biệt**; nếu vẫn giống nhau thì tinh chỉnh câu hỏi rồi chạy lại (lặp đến khi thấy khác biệt — đó là bằng chứng báo cáo).

### 2. Chạy đủ 4 chiến lược cho bảng so sánh nhóm
- Chạy `bench.py` với `CHUNKER` = `fixed`, `sentence`, `recursive` (còn `heading` đã có, 10/10 — là kết quả của bạn).
- Chạy `ChunkingStrategyComparator().compare()` trên 2–3 tài liệu (**đã bỏ frontmatter**) → điền bảng Baseline Analysis (REPORT_NHOM mục 2).
- Từ số liệu 4 chiến lược: điền bảng So sánh giữa thành viên + viết phần "Chiến lược nào tốt nhất & tại sao" + tìm **1 failure case thật** (3 phần: câu hỏi nào hỏng / vì sao / đề xuất sửa) — khả năng cao thấy ở `fixed` làm vỡ bảng Markdown hoặc `sentence` trên file không dấu chấm.

### 3. Hoàn thiện báo cáo
- **REPORT_NHOM**: mục 2 (baseline + chiến lược thành viên + so sánh), mục 3 (Q4 mới + bảng A/B kết quả + trả lời "filter có giúp không"), mục 4 (insights, bài học, failure case).
- **REPORT_CANHAN**: mục 4 (Dự đoán độ tương tự — chọn cặp câu, dự đoán, rồi đo thật bằng `text-embedding-3-small` để điền cột kết quả), mục 5 (bảng kết quả truy xuất của chính bạn — dùng run `heading`: top-1, score, relevant, agent answer), đếm "x/5 câu có chunk liên quan trong top-3".
- Header tên nhóm/thành viên để placeholder cho nhóm tự điền.

### 4. Chuẩn bị nộp (CP7)
- Kiểm `pytest tests/ -v` → 42 passed lần cuối; `git status` không lộ `.env`/`.venv` (đã gitignore sẵn).
- Liệt kê checklist commit: `bench.py`, `ket_qua_benchmark.txt` (bản heading của bạn), corpus, báo cáo. Tên repo `K4-DAY07-HongPhi-...` — phần commit/push để bạn tự bấm (repo là bài nộp cá nhân).

Không đụng: corpus (đã clean), `src/` (42/42), `scripts/`.